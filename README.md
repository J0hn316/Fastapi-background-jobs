# FastAPI Background Jobs

A FastAPI backend project for learning background job processing, worker design, and job lifecycle management.

## Setup

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python run.py
```
